# Default: normalise text files to LF in the repo and the working tree
* text=auto eol=lf

# Windows-native scripts keep CRLF in the working tree
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
